# 开发日志

## v0.1 — 立项与可玩原型

从空仓库到可玩版本：核心循环（移动 / 自动开火 / 拾取 / 升级三选一）、5 种武器、
6 种敌人、母舰 BOSS、粒子与音效、移动端摇杆、GitHub Pages 自动部署。

### 平衡：用无头模拟当测试

浏览器标签页在后台时 `requestAnimationFrame` 被节流，肉眼验证不可靠。
改为直接 import 游戏模块、手动推进 `update(1/60)`，用一个"绕圈机器人"跑批量对局，
把存活时间、等级曲线、屏幕内敌人数当作可回归的指标。三个靠肉眼看不出来的问题因此被抓到：

1. **星尘吸不进来。** 磁力用的是 `pull = 1 - d/r` 的力，在半径边缘趋近于 0，
   星尘被吸到边界就停住，中位距离恒定在 123（吸取半径 125）。
   经验完全断流——60 秒只有 3 级。改成直接驱动速度后，同样时间到 5 级。

2. **屏幕是空的。** 武器索敌距离 900px，而竖屏可视半高只有 ~250px，
   玩家把所有敌人在屏幕外点掉了：43 个存活敌人里屏幕内只有 **1 个**。
   把索敌距离压到可视范围（400/480/560）后，平均屏幕内敌人 1 → 4~8。

3. **敌人追不上。** 玩家 178px/s，杂兵只有 60px/s，敌群被永久甩在身后。
   提高敌人速度并把"掉队敌人"的回收半径从 2200 收到 1000，敌群重新贴脸。

调平后机器人（无脑绕圈 + 随机选卡）中位存活 85 秒，单帧更新峰值 5.7ms。

### 已知待办

- 机器人还够不到 90 秒的母舰，需要更强的中期成长
- 缺少元进度（局外成长）与更多敌人行为
- 音效偏单薄，缺 BGM
