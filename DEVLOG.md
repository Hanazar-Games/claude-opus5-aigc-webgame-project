# 开发日志

## v0.1 — 立项与可玩原型

从空仓库到可玩版本：核心循环（移动 / 自动开火 / 拾取 / 升级三选一）、5 种武器、
6 种敌人、母舰 BOSS、粒子与音效、移动端摇杆、GitHub Pages 自动部署。

### 平衡：用无头模拟当测试

浏览器标签页在后台时 `requestAnimationFrame` 被节流，肉眼验证不可靠。
改为直接 import 游戏模块、手动推进 `update(1/60)`，用一个"绕圈机器人"跑批量对局，
把存活时间、等级曲线、屏幕内敌人数当作可回归的指标。三个靠肉眼看不出来的问题因此被抓到：

1. **星尘吸不进来。** 磁力用的是 `pull = 1 - d/r` 的力，在半径边缘趋近于 0，
   星尘被吸到边界就停住，中位距离恒定在 123（吸取半径 125）。
   经验完全断流——60 秒只有 3 级。改成直接驱动速度后，同样时间到 5 级。

2. **屏幕是空的。** 武器索敌距离 900px，而竖屏可视半高只有 ~250px，
   玩家把所有敌人在屏幕外点掉了：43 个存活敌人里屏幕内只有 **1 个**。
   把索敌距离压到可视范围（400/480/560）后，平均屏幕内敌人 1 → 4~8。

3. **敌人追不上。** 玩家 178px/s，杂兵只有 60px/s，敌群被永久甩在身后。
   提高敌人速度并把"掉队敌人"的回收半径从 2200 收到 1000，敌群重新贴脸。

调平后机器人（无脑绕圈 + 随机选卡）中位存活 85 秒，单帧更新峰值 5.7ms。

### 已知待办

- 机器人还够不到 90 秒的母舰，需要更强的中期成长
- 缺少元进度（局外成长）与更多敌人行为
- 音效偏单薄，缺 BGM

## v0.2 — 把"手感"变成可测的数字

上一版的平衡是靠一次性的浏览器 eval 脚本调的，不可复现也不可回归。这一版先把它变成仓库里的工具，再用它做设计决策。

### 无头模拟器 `tools/sim.mjs`

游戏逻辑本来就不碰 DOM，只需把 `input.js` / `audio.js` 的模块顶层 DOM 访问加上守卫，
再把所有随机收敛到 `util.setSeed()` 的种子 RNG，就能在 Node 里直接 `import` 跑对局。

```
node tools/sim.mjs --runs 20 --bot kite --picks greedy
```

内置两个机器人：`circle`（无脑绕圈，作为下限基准）和 `kite`（按敌人距离加权逃离 +
向最近星尘漂移，作为"会玩的人"的粗略替身）。Node 里跑出的 `circle` 中位存活 88.3s，
与上一版在浏览器里测的 85s 吻合，说明这个替身是可信的。

### 模拟器抓到的第一个设计缺陷：母舰打不死

`kite` 机器人 20 局的报告里有一行刺眼的数字：**母舰刷出 25 只，击杀 0 只。**

原因是自动索敌永远选"最近的敌人"。母舰又慢又肉，身前永远挤满了跑得更快的杂兵，
于是所有单体武器整场都在啃杂鱼，母舰一滴血都不掉。
给追踪导弹和湮灭射线加了 boss 优先级索敌（脉冲枪保持扫场定位），
母舰血量下调、移速上调，并且母舰在场时杂兵刷新减半——击杀率 0/25 → 4/33。

### 精英怪与道具

真正的瓶颈是中期成长乏力。加了两层内容：

- **精英怪**：45 秒后 4.5% 概率生成，4 倍血、1.5 倍体型、0.72 倍速度，8 倍经验，必掉道具
- **道具**：治疗 / 磁暴（全场星尘归位）/ 轨道打击（全屏爆发）；母舰死亡固定掉治疗 + 轨道打击

效果（20 局 kite + greedy）：

| 指标 | v0.1 | v0.2 |
| --- | --- | --- |
| 中位存活 | 160.5s | 193.2s |
| 中位等级 | 11 | 15 |
| 中位击杀 | 340 | 543 |
| 母舰击杀 | 0 / 25 | 15 / 36 |
| 最长单局 | 218s | 433s |

### CI 护栏

部署流水线加了一个 `balance` job，跑 16 局并断言中位存活落在 90~420 秒之间，
不通过就不部署。以后任何一次数值改动都逃不过这道回归。

### 已知待办

- 机器人仍未打到 600 秒上限，后期成长天花板偏低（考虑武器进化 / 局外元进度）
- 精英怪与母舰缺少差异化行为，目前只是"更肉的杂兵"
- 缺 BGM

## v0.3 — 武器进化，以及模拟器抓到的一个老 bug

### 模拟器抓到的 bug：同一帧被打死两次

给棱镜风暴（一次射出 6 道贯穿光束）做验证时，报告里出现了不可能的一行：

```
boss  spawned 18  killed 22
```

击杀数超过了刷新数。原因是 `damageEnemy()` 没有判断目标是否已死：
同一帧内多次命中（星爆 + 子弹、棱镜的 6 道光束）会让血量两次降到 0，
`killEnemy()` 就被执行两次——击杀数、经验球、掉落全部翻倍。

这个 bug 从 v0.1 就存在，只是当时单帧多段伤害少，肉眼完全看不出来，
它还悄悄污染了 v0.2 记录的"母舰击杀 15/36"（那个数字被重复计数抬高了）。
修复是一行 `if (e.dead) return;`，另外补了一个针对性回归脚本验证同帧两次致命伤只结算一次。

### 武器进化

5 种基础武器各自对应一个进化：满级 + 取到配对词条，卡池就会出现金色进化卡。
卡池还会对"能解锁进化的那个词条"加权，避免玩家卡在差一张的状态。

调这个功能时踩了两次过头：

1. 第一版进化几乎不可能触发（20 局只出现 1 次）。根因是**经验曲线的成本是等级的二次函数，
   而收入恒定为每杀 1 点**——等级越高越卡死，根本攒不到进化需要的投入。
2. 改成线性成本 + 经验随时间成长后直接失控：300 秒封顶的对局 6/8 局打满，等级 104，
   5 个进化全部集齐。

最后落在"成本 `5 + 4L + 0.35L²`、收入随时间 `×(1 + t/150)`、基础武器满级从 6 降到 4、
武器槽从 5 降到 4"。武器槽和满级同时收紧是关键——槽位太多会让升级被摊薄，
永远凑不出一条完整的进化线。

### 后期跑飞与解法

进化生效后出现了新问题：一旦成型玩家就无敌了，12 局里 6 局打满 480 秒上限。
反直觉的是，**把敌人血量调高反而让游戏更简单**——敌人活得久 = 场上尸体多 =
范围进化武器收割到的经验更多，雪球滚得更快。

真正的原因是成型后玩家能清空自己周围的一切，近战杂兵不再构成威胁。
解法是后期加大远程敌人权重（210s / 270s / 330s 三档），子弹不会被 AoE 拦下来，
这才是唯一还能威胁成型玩家的东西。

### 数据（12 局 kite 机器人）

| 指标 | v0.2 | v0.3 |
| --- | --- | --- |
| 中位存活 | 193.2s | 182.4s |
| 中位等级 | 15 | 15 |
| 打满上限 | 0/20 | 0/12 |
| 进化次数 | — | 8 次 / 12 局，5 种全部出现 |

另外新增 `focused` 选卡策略（一条线走到黑）：中位 191.6s，10 局里 4 局打满上限。
集中培养明显强于雨露均沾，说明"该往哪投"变成了一个真实的决策。

### 已知待办

- `focused` 打法仍能在后期跑飞（4/10 打满上限），后期压力还不够
- 精英怪仍然只是"更肉的杂兵"，没有差异化行为
- 缺 BGM

## v0.4 — 精英词缀，以及一次真正的性能回归

上一版留了两个待办：精英怪只是"更肉的杂兵"，以及 `focused` 打法后期跑飞。
这一版发现这两件事其实是同一件事——精英词缀正是后期压力最合适的载体。

### 三种词缀，各自针对成型 build 的一个假设

| 词缀 | 行为 | 针对的假设 |
| --- | --- | --- |
| 🟢 分裂 | 死亡时炸成 4 只杂兵 | "清空周围就安全" |
| 🔴 齐射 | 周期性打出一圈子弹 | "范围武器能拦下一切" |
| 🔵 加速 | 持续加速直到追上你 | "跑得比它们快就行" |

精英出现概率也随时间上升（`0.035 + t/14000`）。屏幕上用彩色虚线环区分词缀。

一个实现上的坑：分裂出来的杂兵会再走一遍 `spawnEnemy()`，因此**它们自己也可能被
roll 成精英分裂怪**，理论上可以指数级链式分裂。加了 `forceNormal` 参数堵掉。

### 性能回归：单帧 17.7ms

词缀上线后模拟器报告里 `peak update` 从 1.8ms 跳到 **17.7ms**——超过 16ms 的一帧预算，
真机上就是肉眼可见的卡顿。

根因不在词缀，而在"蜂群"进化：它一次齐射 6 枚导弹，每枚都调用
`nearestEnemy(idx)` 找第 N 近的目标，而那段代码的实现是**把范围内所有敌人收集成数组再整体排序**。
后期场上 380 个敌人时，一帧里就是 6 次上百元素的分配 + 排序。

改成固定大小缓冲区的 top-K 插入选择（k = idx+1 ≤ 6），无分配、无排序：
**17.7ms → 3.1ms**。另外补了一个针对性验证：把敌人摆在 100~800 的已知距离上，
逐个检查第 N 近的返回值，并覆盖超出射程被截断和空场两种边界。

这个 bug 值得记一笔：它从 v0.1 就存在于代码里，但只有当"齐射型进化 + 后期高密度"
同时出现时才会暴露。模拟器把整局对战压缩成几秒钟跑完，才让这种尾部场景变得可观测。

### 母舰节奏递进

母舰是唯一血量随击杀数成长的敌人，所以让它的间隔逐次收紧
（`90 - 9×已出现次数`，下限 42 秒），作为终结无限局的手段。

### 数据

| 指标 | v0.3 | v0.4 |
| --- | --- | --- |
| greedy 中位存活 | 182.4s | 174.1s |
| greedy 打满上限 | 0/12 | 1/12 |
| focused 中位存活 | 191.6s | 158.1s |
| focused 打满上限 | 4/10 | 3/10 |
| 单帧峰值 | 1.4ms | 3.1ms |

`focused` 的分布是明显的双峰：要么在成型前就死（p25 147s），要么滚起雪球打满上限。
这其实是想要的结果——集中一条线是高风险高上限的打法，而不是无脑最优解。

### 已知待办

- `focused` 仍有 3/10 打满上限，后期终结手段还不够硬
- 缺 BGM
- 结算界面可以展示本局进化与母舰击杀数

## v0.5 — 收尾：生成式配乐、结算信息、以及一个诚实的结论

### 配乐也是算出来的

整个项目没有任何素材文件，配乐也不例外：一段 A 小调四小节走向（Am–F–C–G），
用 WebAudio 的前瞻式调度器（每 40ms 检查一次，向前排 200ms）逐个音符排进时间线。
音层随强度递进：贝斯 → 脉冲 → 琶音 → 高音对位，强度由存活时间映射（150 秒到满）。

无头浏览器没有扬声器，所以验证方式是**拦截 `createOscillator` 统计排进去的音高**：
10 秒内排了 49 个音，低音部恰好覆盖 43.66 / 49 / 55 / 65.41 Hz——
正是 F–G–A–C 四个根音，走向完整循环，静音开关也生效。

### 结算界面

现在会展示本局最终配装（进化武器用金色高亮）、母舰击杀数与进化次数。

### 又一次"击杀数超过刷新数"

深局给母舰加了护航（第 4 只之后成对出现）后，报告里再次出现 `spawned 29 killed 35`。
这次不是游戏 bug 而是**统计口径 bug**：`bossCount` 是"第几波"，用于血量成长和间隔收紧，
一波刷 2 只时它就不再等于实际刷新数了。拆成 `bossCount`（波次）和 `bossSpawns`（实际只数）。

同一个症状在 v0.3 出现过一次，那次是真的重复结算。这次提醒了我：
**报告里的不变量（击杀 ≤ 刷新）比报告里的数值更有价值**，它不需要我判断"这个数字合不合理"。

### 五个版本的最终数据

| | v0.1 | v0.2 | v0.3 | v0.4 | v0.5 |
| --- | --- | --- | --- | --- | --- |
| greedy 中位存活 | 160.5s | 193.2s | 182.4s | 174.1s | 174.1s |
| 中位等级 | 11 | 15 | 15 | 17 | 17 |
| 单帧峰值 | 5.7ms | 4.6ms | 1.4ms | 3.1ms | 1.3ms |
| 内容 | 5 武器 6 敌人 | +精英 +道具 | +5 进化 | +3 词缀 | +配乐 |

### 诚实的遗留问题

- **`focused` 打法仍有 3/10 打满 480 秒上限。** 试过三种手段（后期远程敌人加权、
  母舰间隔收紧、母舰护航），都没能真正压住成型后的雪球。根本原因是玩家的伤害成长
  是乘性的（伤害% × 攻速% × 暴击 × 进化），而敌人成长是加性的。要真正解决需要重做
  数值框架，不是调参能解决的。
- 没有局外元进度（meta progression），重复游玩的动力只有刷纪录。
- 敌人种类偏少（6 种），后期靠数量而非质量施压。

### 这五个版本里最有用的一件事

不是任何一个功能，而是 v0.2 那个**无头模拟器**。它抓到的四个问题——
星尘磁力在半径边缘失效、武器索敌超出屏幕、母舰因索敌逻辑永远打不死、
同帧重复结算击杀、nth-nearest 索敌的 17.7ms 帧峰值——
没有一个是靠肉眼玩几局能发现的。

它们的共同点是：**症状要么完全不可见（数字悄悄错了），要么只在分布的尾部出现
（后期高密度 + 特定 build）。** 把一局 8 分钟的对战压缩成几秒钟、批量跑几十局、
并且对报告里的不变量下断言，这类问题才会自己浮出来。

## v0.6 — 把"调参解决不了"的那个问题解决掉

v0.5 收尾时我把 `focused` 打法跑飞定性为"根因是玩家伤害乘性成长 vs 敌人加性成长，
需要重做数值框架而非调参"，然后没做。这一版把它做完。

### 匹配增长的"形状"，而不是继续加常数

玩家的 DPS 是**连乘**出来的：伤害% × 攻速% × 暴击 × 武器等级 × 进化。
每次升级都是乘一个系数，而升级次数大致随时间线性增长，所以玩家战力 ≈ 指数增长。

而敌人血量一直是加性的 `1 + t/80 + (t/190)²`——多项式增长。
指数早晚碾过多项式，所以只要玩家活过某个点，游戏必然失控。之前三次尝试
（后期远程加权、母舰间隔收紧、母舰护航）都是在多项式上加项，方向就是错的。

改成同样的形状：`hpMult = 2^(t / hpDouble)`，血量每 `hpDouble` 秒翻倍。

### 用扫描代替拍脑袋

顺手给模拟器加了 `--sweep`：把整个批次在某个 `TUNE` 旋钮的多个取值上各跑一遍。

```
sweep hpDouble · 8 runs each · bot=kite picks=focused cap=480s

  value   p25     median  p75     lvl  bossK  cap
  70      121.2   157.2   253.7   11   9      0/8
  85      125.7   156.4   480.0   12   20     2/8
  100     148.2   480.0   480.0   84   51     5/8
  120     200.4   480.0   480.0   83   44     4/8
```

跑飞是个**阈值现象**而不是渐变：85 到 100 之间，中位存活从 156 秒直接跳到打满上限，
中位等级从 12 跳到 84。这种断崖只有把整条曲线摆出来才看得见。

最终取 78，三种策略的表现：

| 策略 | 中位存活 | 打满上限 |
| --- | --- | --- |
| circle（无脑绕圈） | 88.4s | 0/10 |
| greedy（雨露均沾） | 177.0s | 0/10 |
| focused（一条线走到黑） | 152.3s | 1/12 |

16 局完整批次：中位 166.9s，最长 213.5s，**0/16 打满上限**。跑飞问题消失。

代价是诚实的：`focused` 不再拥有更高的上限，它现在换来的是母舰击杀数（13 对 5）
而不是更长的存活。策略差异从"存活长度"变成了"擅长什么"，比之前扁平一些。

### 把教训写成断言

v0.5 我总结过"报告里的不变量比数值更有价值"。这一版把它落实成代码：
模拟器现在断言 `母舰击杀 ≤ 母舰刷新`、运行时长有限且非负、等级不低于起始值。

这类检查不需要我判断"这个数字合不合理"。为了确认它真的会触发，
我故意把 `bossKills++` 改成 `+= 2`，模拟器立刻报：

```
FAIL: invariant violated
  seed 1: killed 4 bosses but only 2 spawned
```

——正是 v0.3（同帧重复结算）和 v0.5（波次计数口径）两次咬到我的那类 bug。

## v0.7 — 一次彻底的自检

这一版没有加玩法内容，只做一件事：把前面几版新加的东西（配乐、结算、词缀、进化、指数血量）
逐个当作"可疑对象"重新审一遍，能写成脚本验证的就写脚本，不靠读代码下结论。

### 先证伪了自己的一个猜测

我最先怀疑的是**音效刷屏**：`sfx.kill()` 每次击杀都触发，后期一秒几百杀，
理论上会产生大量 WebAudio 节点。但写脚本统计真实对局里每秒的音效触发数后：

```
total sfx calls: 455
PEAK sfx calls in one second: 16
```

峰值只有 16 次/秒。原因是击杀集中在少数几帧、而 `sfx.hit()` 本来就有 35% 概率节流。
**猜测被数据否掉了**，没有据此改任何代码。

### 抓到的四个真问题

**1. 进化后还能把原武器当"新武器"再买回来。** 用 400 次抽卡统计：

```
blaster offered as NEW in 104/400 rolls while Starbreaker is owned
```

26% 的抽卡会把已经进化掉的脉冲枪再摆出来。玩家拿了就是用一个宝贵槽位换一个
严格更弱的版本——一个纯粹的陷阱选项。修复：卡池排除"进化体已拥有"的基础武器。

**2. 开局前静音，音乐照样响。** `setMuted()` 只在 `musicGain` 已存在时改增益，
而 `startMusic()` 创建 `musicGain` 时把增益写死成 0.34，把静音状态覆盖掉了。
在标题界面勾了静音再开局 → 音乐全音量播放。

**3. 暂停后音乐继续播。** 包括切到别的标签页触发自动暂停时——游戏停了，音乐还在响。
新增 `setMusicPaused()`：停止调度器并把音乐总线增益归零。

**4. 静音设置不持久。** 刷新页面就恢复出鸣。改为写入 localStorage，
并且把静音开关同时放到标题界面（之前只有暂停菜单里有，开局前根本够不着）。

另外顺手修了：道具拾取复用了升级音效（治疗听起来和升级一模一样），
现在治疗/磁暴/轨道打击各有各的声音；删掉了 `game.js` 里一个没人用的 `AFFIX_COLOR`。

### 验证时踩的坑：我第一次"验证通过"是假的

改完音频后我跑验证，结果三项全部还是原样。差点以为修复逻辑写错了，
于是先去查"到底加载的是哪份代码"：

```
audioExports: audio music setMuted sfx startMusic stopMusic unlockAudio
hasLoadMuted: false
```

浏览器缓存了旧的 `audio.js` 模块——HTML 是新的（补丁面板已经在 DOM 里了），
但 ES 模块走的是 HTTP 缓存。**我实际上是在对旧代码做验证。**
用 `fetch(f, {cache:'reload'})` 逐个刷新后重测才拿到真实结果。

还有一个方法论错误：我一开始用"统计 `createOscillator` 调用数"来判断静音是否生效。
但静音是把增益设为 0，振荡器照常创建——这个指标根本测不了静音。
改成直接读音乐总线的 `gain.value`（0 = 静音）才是对的观测量。

### 顺带补的 UI/UX

- 升级卡支持 `1` / `2` / `3` 键选择
- 新增**补丁公告面板**：当前版本展开在上，历史版本收进可折叠的 "Earlier versions"
- 标题界面显示版本号徽章

### 回归

16 局 greedy：中位 166.9s，0/16 打满上限，单帧峰值 1.8ms——与 v0.6 一致，
卡池改动没有影响平衡。移动端（375×812）布局已确认正常。

## v0.8 — 第二轮自检：一个"升级反而变弱"的武器

延续 v0.7 的做法，把每个怀疑都写成可执行的检查。这轮**先否掉了两个猜测，再抓到一个真问题**。

### 否掉的两个猜测

**猜测一：高速弹丸会穿透敌人。** 湮灭射线速度 1500px/s，一帧走 25px，
而敌人半径 11 + 弹丸半径 6 只有 17px 的命中窗口——理论上应该会漏判。
写了个"正前方摆一个静止靶子，看能不能打中"的测试：

```
weapon    speed  px/frame  hit-window  connects?
laser     1500   25.0      18          12/12
railgun   1500   25.0      21          12/12
prism     1500   25.0      17          12/12
```

全部 12/12 命中。原因是子弹每帧都做一次范围查询，而查询半径是 `b.r + 24`，
比单帧位移大，实际上等价于做了扫掠检测。猜测错误。

**猜测二：粒子/浮动文字/敌人子弹会无限增长。** 把暴击率强行拉到 60% 跑满一局统计各池峰值：

```
peak: enemies 90, bullets 2, ebullets 7, orbs 9, parts 87, texts 17, novas 1
```

全部有界。猜测错误。

### 抓到的真问题：脉冲枪升到 Lv.3 反而变弱

同一个测试里，**最慢的那把武器反而只有 6/12 命中**。按等级拆开看：

```
lv  bolts  120px 200px 280px 360px
1   1       hit   hit   hit   hit
2   1       hit   hit   hit   hit
3   2       hit   hit   MISS  MISS
4   2       hit   hit   MISS  MISS
```

Lv.1~2 是单发，直接瞄准圆心，全距离命中。Lv.3 起变成两发，
代码用的是**固定角度** `spread = 0.14` 左右分开——于是两发都不在准星上，
各偏 0.07 弧度。距离越远横向偏移越大：280px 处偏 19px，已经超出 15px 的命中窗口，
**两发全部擦身而过**。而这把枪的索敌距离是 400px，等于三分之一的射程是废的。

也就是说：**把起手武器升级，它在中远距离的输出反而归零了。** 这是最糟的一类数值 bug，
而且发生在每局都会用到的初始武器上。

修法是把"固定角度"换成"在目标处保持固定横向间距"：
`spread = atan2(9, dist)`。不管远近，两发始终各偏目标 9px，稳定落在命中窗口内。
修复后 4 个等级 × 4 个距离全部命中。

### 一个没有过度反应的决定

修完之后跑回归，16 局里出现 **1 局打满上限**（v0.6 是 0/16）——看起来像是我把武器改强了。
但对 `hpDouble` 做扫描时结果是非单调的（70→1/10，74→3/10，78→1/10），
说明这个样本量下**噪声压过了信号**。于是把样本加到 24 局：

```
reached cap 1/24
```

4% 的打满率，与 v0.6 的 0/16 在统计上无法区分。所以**我没有据此调任何数值**——
命中率修复本来就是让武器回到它伤害数值一直假设的样子，为一个噪声去补偿反而会引入真的偏差。

### 配乐：开局二十多秒几乎是静音

`intensity = t/150`，而脉冲层要 `k > 0.15` 才进来，也就是**前 23 秒只有每 1.15 秒一记贝斯**。
更糟的是对位层要 `k > 0.6`（t=90s）、满编要 t=150s，而中位对局只有 ~160 秒——
大多数玩家从没听到过完整编曲。

改成 `0.2 + 0.8 × min(1, t/110)`：一开局就有贝斯 + 脉冲的律动，
琶音 21 秒进来，对位 55 秒进来，110 秒满编。

### 顺带修的

- `1 / 2 / 3` 快捷选卡是 v0.7 加的，但卡面上没有任何提示，等于没人会发现——现在每张卡左侧有按键角标
- 补丁公告之前只能从标题界面进；`prevPanel` 里那个"从暂停菜单进入"的分支是**永远走不到的死逻辑**。给暂停菜单补了入口，这条分支才真正生效
- 版本号徽章会在有未读版本时显示红点，读过后写入 localStorage
- 文案：`Fires 1 bolt(s)` 的蹩脚复数，以及 `lv >= 5` 这个满级 4 永远触发不了的死分支

### 回归

24 局 greedy：中位 155.3s，1/24 打满上限，单帧峰值 2.2ms。CI 门禁与不变量断言全部通过。

---

## v0.9 — 给武器建一个基准台

`sim.mjs` 回答的是「一局该多长」。这个问题它答得很好，但有一整类问题它天生看不见：
**某一把武器是不是兑现了它卡面上的承诺。** v0.8 那个「脉冲枪升到 Lv.3 反而变弱」
就是典型——存活时间的噪声远大于一把武器的贡献，中位数根本不会动。

于是新建 `tools/bench.mjs`，每把武器每个等级两个数字：

- **boss**：120px 处一个免疫击退的靶子，20 秒的 dps。这就是母舰战。
- **clear**：真实对局里的击杀/秒。活的刷怪、会风筝的机器人、玩家不死、
  敌群数量被钉在 60 只，跳过前 60 秒。

### 靶场自己先被推翻了三次

第一版靶场是个 24 只敌人的同心环，半径 70/140/210/280。结果轨道刀 Lv.3
报 **0 dps**。查下去发现是环距太粗：Lv.3 的刀环半径 95px，正好落在 70 和 140
之间的空档里，而命中窗口只有 23px。**是靶场的问题，不是武器的问题。**

第二版换成 6px 步进的黄金角螺旋，覆盖每一个半径带。但这又对直线武器不公平——
真实对局里敌人是朝你收敛的，天然会排成放射状的线。

第三版干脆从真实对局里截一帧敌群下来当靶场。结果更糟：截取时机器人在全速绕圈，
420px 内只有 5 只敌人在 150px 以内，轨道刀还是几乎测不到。

结论是**冻结的战场根本测不了轨道类武器**——它们的输出来自敌人「流过」刀环，
而不是「站在」刀环里。最终版放弃冻结：真实对局、玩家不死、敌群钉在固定密度。

同样地，单体靶一开始被星爆的击退轰飞了，星爆读数 2 dps。真实的母舰免疫击退——
**把靶子标记成 boss** 之后，星爆从 2 变成 10，索敌偏好 boss 的武器也终于会瞄它。

### 抓到的三个真问题

**一、Starbreaker 是平级替换。** 脉冲枪进化后 202 单体 dps / 2.71 清场，
而满级脉冲枪是 206 / 2.62。它要吃掉一把满级武器**外加**一次属性选择，
换来的是 0.98 倍和 1.04 倍。其余四个进化是 2.0~3.8 倍。

**二、轨道刀完全无视攻速。** 重击冷却 `e.orbCd = 0.32` 是写死的常数。
`--stats` 表里那一格是干净的 **0.0%**——「Rapid Cycling：攻击间隔 -11%」
对这整条武器线的收益是零，而卡面上没有任何提示。

**三、治疗卡在 27000 次发牌里出现 0 次。**「Emergency Supply」只挂在
`if (!out.length)` 这条兜底分支上，而卡池永远有 10 张属性卡，三张牌永远填得满。
这个分支不可能执行。

### 一个不是「数值弱」而是「打法依赖」的问题

轨道刀满级对风筝机器人 0.57 击杀/秒，对硬冲机器人 3.08——**5.4 倍的差距**。
它不是弱，是它的强度完全由打法决定，而游戏的主流打法（风筝）恰好是它最差的那个，
并且没有任何东西提示玩家这件事。

直接加数值只会让硬冲流溢出。改成：**移动时刀环拖在身后**，扫过你正把敌群
拽过去的那条尾流。风筝 0.57 → 1.49，硬冲 3.08 → 3.09，差距从 5.4 倍收到 2.1 倍。

### 副作用，以及为什么要扫描

这些增强让 `focused` 策略 6/16 局打满 600s 上限（v0.8 是 2/16）。
逐项拆开测，治疗卡和 Starbreaker 各贡献一半。扫描 `hpDouble` 后 78 → 66，
尾部回到 v0.8 的水平。

---

## v1.0 — 这个游戏一直缺一个结局

翻一遍前面八篇日志会发现一件事：**从 v0.3 到 v0.9，每一次平衡工作都在做同一件事——
阻止强 build 无限打下去。** v0.6 把敌人血量从加性改成指数，v0.9 又把 hpDouble
从 78 拧到 66。每次我加强任何东西，尾部就有一批对局变成打不完。

这不是数值问题。**无尽模式必须靠数字压住，而一条弧线是因为被设计成会结束才结束的。**

### 结构

一局是一场战役：四幕，7:00 时「The Devourer」到场，杀掉它就赢。

| 幕 | 起始 | |
| --- | --- | --- |
| Debris Field | 0:00 | 有空间建立 build |
| The Hunt | 2:10 | 堡垒、喷吐者、编织者，母舰开始有分量 |
| Swarm Tide | 4:30 | 密度、成团、带词缀的精英 |
| The Devourer | 7:00 | 只有这一场，杂兵刷新降到五分之一 |

最终 Boss 三件事让它成为一场「战斗」而不是一只更胖的母舰：
螺旋弹幕逼你持续移动而不是绕圈、66% 以下召唤护卫惩罚没有清场手段的 build、
以及**随战斗时钟增长的暴怒**——伤害与速度在 150 秒里持续爬升。
最后这一条是对局必然终止的保证：**拖不是战术，只是输得慢一点。**

同时，时间驱动的敌人缩放**在 420s 冻结**。最后一战是一个被设计好的遭遇，
而不是曲线上又一格；不冻结的话，拖住 Boss 会让杂兵自己变得不可生存。

三个难度层：Recruit / Veteran / Nightmare（打通 Veteran 解锁）。

### 两个把游戏变成「阈值系统」的老设计

改成战役之后，第一次扫描的结果非常刺眼：

```
xpCurve  win%    median   lvl
0.35     58.3%   486.2    75
0.8       0.0%   208.2    20
```

**曲线上一个 0.1 的差别，把通关率从 58% 打到 0%。** 这不是曲线，是悬崖。
中间没有任何过渡：要么滚雪球滚到 75 级无敌，要么 180 秒死掉。

两个原因，都是老设计：

**一、属性卡是乘法叠加。** `p.damage *= 1.15` 叠 20 次是 16 倍。
玩家强度对等级是指数的，所以两级的差别就能决定一整局，
而敌人曲线必须同样刀锋般锐利才能压住它。改成加法叠加：20 次 +18% 是 4.6 倍。

**二、升级曲线是二次的。** `xpNext = 5 + 4L + 0.35L²`，于是「清得快 → 经验多 →
更强 → 清得更快」这条正反馈几乎不衰减。改成指数 `7 × 1.16^L`：
等级对清场效率的响应变成对数的，清场速度翻倍只多给 5 级，而不是 40 级。

改完之后曲线才有了梯度，通关率对旋钮的响应从悬崖变成斜坡。

### 最终 Boss 的血量不再由别的旋钮间接决定

`hpMult` 是 `2^(t/hpDouble)`，最终 Boss 一旦走这条公式，
**每次我调杂兵曲线，最后一战的长度就跟着变**。改成显式的 `TUNE.finalHp`。
它是「一场被设计出来的战斗有多长」，不该是别人的副产品。

### 模拟器的主指标换掉了

存活时间只是难度的代理指标，而且是个差的代理：
它分不清「在爬坡途中 5:00 死掉」和「在最终 Boss 手上 5:00 死掉」，
**而这两件事要往相反的方向修**。现在报告直接给：

```
  WON        9/40  (22.5%)   clear 492.2s, of which 72.2s was the Devourer
  reached    12/40 got to the Devourer at 420s
             the 3 that lost there left it at 26.9% hp (median)
  died in    debris field 1 · the hunt 15 · swarm tide 12 · the devourer 3
```

40 局实测：

| 层 | 通关 | 走到最终 Boss | 死在它手上 | 最终战长度 |
| --- | --- | --- | --- | --- |
| Recruit | 67.5% | 29/40 | 2 | 45s |
| Veteran | 22.5% | 12/40 | 3 | 72s |
| Nightmare | 5.0% | 12/40 | 10 | 99s |

三个层的失败**位置**不同，而不只是概率不同：Recruit 走到 Boss 面前基本就赢了，
Nightmare 走到的人有 10/12 倒在那里。**同一场战斗，在不同的层里是不同的问题**——
这正是「通关率」比「存活时长」值钱的地方，后者会把这两种失败混成一个数字。

这个形状不是一次就调对的。第一版 `finalHp` 是 120000，结果是**到达最终 Boss 的人
全部通关**——Boss 成了胜利巡游，整个「对局以一场真正的战斗收尾」的立意落空。
加血只让它变长不让它变危险（暴怒斜坡 150 秒，而实战只有 70 秒，玩家只吃到 47%）。
把斜坡收到 95 秒之后，压力才落在战斗窗口之内。

新增不变量：**任何一局都不许跑到硬停时限**——那意味着弧线没有终止。

### 收尾时发现的那个 bug，比这一整版都严重

做完之后顺手跑了一次不同视口的通关率对比：

```
  420x780    win 59.4%
  900x620    win 31.3%
  1600x900   win  0.0%   ← ？
```

1600x900 是 0/32。查下去是这一行：

```js
if ((e.x - p.x) ** 2 + (e.y - p.y) ** 2 > 1000 * 1000 && !e.boss) { ...回收... }
```

刷怪环是 `视口对角线 / 2 + 110`，而回收半径是写死的 **1000px**。
1600×900 的对角线是 1836px，刷怪环 1028px——**敌人一出生就在回收半径之外**，
于是每一帧都被传送回环上，永远向玩家靠近不了 28px。

```
  view        ring   60 秒后：最近的敌人 / 击杀 / 等级
  900x620     656         3px  kills   56  lv 6
  1600x900    1028     1028px  kills    0  lv 1
  1920x1080   1211     1211px  kills    0  lv 1
  2560x1440   1579     1579px  kills    0  lv 1
```

**在 1080p 显示器上最大化窗口，这个游戏从 v0.1 起就是完全玩不了的。**
零击杀、零升级、敌人悬在屏幕边缘一动不动。

它九个版本没被发现的原因很具体：**模拟器的默认视口是 900x620，
而 900x620 的对角线正好在阈值之下**。所有的批量对局、所有的截图，
都是在那个尺寸或更小的尺寸下取的。工具本身成了盲区。

修法是回收半径必须由刷怪环推导，不能是常数。CI 现在把 1920x1080 也纳入门禁——
这条门禁本来就能抓到它。

### 难度不该由窗口大小决定

同一次对比暴露了第二个问题：即使修好上面那个 bug，
桌面 31% / 手机 59% 的差距仍然在。原因是

> **常驻敌人数 = 刷新率 × 飞行时间，而飞行时间就是屏幕。**

900x620 常驻 77 只，420x780 只有 47 只。刷新率现在按 `参考环 / 当前环` 缩放，
把屏幕这个变量消掉。

修完之后：手机 31.3% / 900x620 22.5% / 1080p 9.4%。
差距从「50 个百分点、且一端完全不可玩」收到 22 个百分点。

**剩下的 22 点没有修掉。** 试过缩短星尘的自动归位延迟（`orbHome` 7→4→2.5），
扫描结果全是噪声，说明机制不在那里。与其猜，不如把它写下来并加进 CI 门禁：
现在手机与 1080p 两个视口都有通关率上下限，
任何让某个尺寸变得不可玩的改动都会当场失败。

### 顺带

- 结算区分 DEVOURER DOWN 与 SIGNAL LOST，通关记录按难度层分别保存，
  且**通关永远压过一次更长的失败**（7:42 阵亡不会顶掉 7:31 通关）
- HUD 多了一条幕进度条；最终战时它让位给 Boss 血条，避免两处都写着 THE DEVOURER
- 最终 Boss 有独立的外观：一圈随掉血收紧的封锁环，让它一眼就不是「更大的母舰」
- 结算显示总伤害量

---

## v1.1 — 打捞，以及一次「代价被玩家换成了收益」

先做了一轮 UI / UX / 音效 / 配乐的自检，然后做了这个项目第一个真正意义上的新系统。

### 自检抓到的：一帧里 59 个音效

统计真实对局里每个音效的触发频率，最糟的单帧是 **59 次发声，其中 55 次是击杀噪声**——
一次轨道打击清掉一群敌人，每一只都在分配自己的 AudioBuffer 和节点图。
浏览器里实测这 300 次调用要 **16.5ms**：光音频建图就吃掉一整帧。

而且它根本不好听。55 个相同的爆裂音挤在 16ms 里不会被听成「55 个击杀」，
只会被听成一记削顶的咔哒声。

修法不是简单丢弃重复，而是**把被吞掉的次数折进下一次并让它更响**——
清场因此落成一记有分量的重击。同样 300 次调用现在是 0.1ms。

顺带发现**通关是没有声音的**。对于整局游戏唯一的目标来说，这是混音里最响的一个 bug。

还有：最后一战听起来和第三分钟一模一样（强度到 1 时所有层早就开着，
而和弦从头到尾没变），以及配乐**完全没有节奏声部**。

### 新系统：为什么是打捞

这个游戏叫 Starfall **Scavenger**，而十一个版本里没有任何东西可以打捞。
但真正让我决定做它的不是主题，是数据：

> 这个项目产生的每一个数字都在说同一件事——**永远后撤**。
> 风筝机器人赢过硬冲机器人；轨道刀对风筝流是 0.57 击杀/秒，对硬冲流是 3.08。
> v1.0 我给刀环加了拖尾来缓解症状，但根因一直没动：**游戏从来没有给过一个站住不动的理由。**

残骸就是那个理由。站进它的环里读条，出环则回退（但比积累慢，闪避应该有代价而不是清零）。

### 代价的第一版是错的

第一版的代价是「信标把增援叫到残骸旁边」。扫描 `salvageHeat` 1.6 / 2.4 / 3.2，
通关率是 **81% / 87% / 81%**——加敌人不但没有惩罚，**通关率还上升了**。

原因很直白：**在一个成型玩家清场快过刷新的游戏里，多出来的敌人就是多出来的经验。**
一个玩家能换算成收益的代价，不是代价。

改成扣玩家自己的输出：**打捞时反应堆被切走，武器射速降到约 55%**。
你最虚弱的那几秒，正好是敌群围上来的那几秒。这个代价随 build 变强而变贵，
因为你放弃的绝对 DPS 更多。

### 一个 NaN 让武器悄悄停火

`salvageChoke` 这个旋钮我加进了读取处，却没加进 `TUNE` 对象。于是
`s.cd * p.rate * undefined` 是 `NaN`，`Math.max(0.06, NaN)` 还是 `NaN`，
而 `w.t >= NaN` 永远为假——**打捞时所有武器直接停火**。

四次不同 `choke` 值的扫描跑出完全一样的数字，这才让我发现旋钮根本没被读到。
通关率看起来始终「合理」，模拟器没有报任何问题。

现在模拟器断言**玩家的任何一项属性都不得是非有限数**。这类 bug 的特征是
它不会崩溃、不会报错，只会让某个系统安静地不工作。

### 「值得拿」和「不拿就没法玩」不是一回事

做完之后我问了一个必须问的问题：**打捞到底值不值？**
把刷新时机也做成旋钮，同种子直接对照：

```
                通关率    走到最终 Boss
  有残骸        34.4%     22/32
  无残骸         0.0%      0/32
```

不是决策，是必修课。基线难度被我按「全部拿满」调过了，
于是不打捞的对局连最终 Boss 都到不了。

削了模块强度、拉低刷新频率、重新扫 `hpDouble` 之后：

| | 通关率 | 走到最终 Boss |
| --- | --- | --- |
| Recruit 不打捞 | 21.9% | 11/32 |
| Recruit 打捞 | 71.9% | 25/32 |
| Veteran 打捞 | 43.8% | 19/32 |
| Nightmare 打捞 | 12.5% | 8/32 |

**Veteran 不打捞仍然是 0%。** 我没有继续把整体难度往下压去凑那个数字——
再压下去 Veteran 打捞流就要到 65% 了。所以我把设计意图写清楚并按它设门禁：
**残骸在 Recruit 是可选的，从 Veteran 起是战役的一部分。**

顺带发现原来那条「跳过残骸仍可通关」的 CI 门禁**什么都没检查**：
`--sweep` 模式在断言之前就 `process.exit(0)` 了。补了真正的 `--tune`。

### 两个模态撞在同一帧

升级和打捞都是模态，而且都可能在同一次 `update()` 里被抬起。
原来是直接赋值 `G.state`，于是后一个会覆盖前一个，
**没被回答的那个offer 就永远消失了**。改成队列，并且卡面在**展示时**才 roll，
这样第一个选择的结果会反映在第二个里。

正常对局 420 秒里没有自然触发过（升级曲线是指数的，同帧两次很罕见），
所以我没有去找触发条件，而是构造了那一帧来证明修复有效。

### 收尾验证时又挖出两个视口 bug

按惯例最后在真实浏览器里跑完整对局做验收。四个种子里两个在 **22~30 秒**就死了，
等级 3、只有一把武器——而同种子的无头模拟活到 342 秒。

查下去发现预览标签里 `innerWidth` 是 **0**。`main.js` 的 `resize()` 只在加载时跑一次，
于是 `G.view` 永久停在 0×0，刷怪环 = `hypot(0,0)/2 + 110` = **110px**——
敌人直接在你的反应时间之内生成。

**真实场景完全可达**：在后台标签页里打开游戏，再切回来。
和 v1.0 那个 1080p 的 bug 是同一类——一个由视口推导、却没有任何下限保护的量。

修法三处：`resize()` 有回退值、`visibilitychange` 时重新测量、刷怪环加下限 420px。
CI 加了 `--view 1x1` 门禁。

### 「同一个种子逐帧重放」这句话，十一个版本以来在浏览器里是假的

修完之后浏览器和无头**还是**对不上。逐帧对比找到了原因：

```js
const sx = cam.shake ? rand(cam.shake, -cam.shake) : 0;   // render.js
```

**渲染层在消耗模拟用的随机数流。** 震屏抖动、尾焰长度都走 `util.random()`，
于是**画一帧就推进一次世界的随机数**。无头模拟从不渲染，所以

> 「所有随机走 `setSeed()`，同一个种子逐帧重放」——**在 Node 里成立，在浏览器里从来不成立。**
> 同一个种子会因为画了多少帧而跑出不同的对局。

给渲染层一个独立的、不受种子控制的抖动源之后再对比：

```
  54s  headless 1749.269/110/lv9   browser 1749.269/110/lv9   ← 完全一致
  55s  headless 1809.938/114/lv9   browser 1809.936/114/lv9   ← 差 0.002px
```

**逐帧完全一致地跑了 54 秒**，第 55 秒开始出现 0.002px 的差。
这一步不是 bug：`Math.hypot` / `Math.atan2` 在不同 JS 引擎之间并不要求逐位相同，
而这是个混沌系统，微小差异最终会放大成完全不同的对局。

所以 README 里那句话被改成了它实际成立的样子：**在同一个引擎内精确重放**，
并把这次的测量结果写进去，而不是继续声称一个更强的性质。

顺带修掉的：升级卡的三张牌在模拟器里被 roll 了两次（一次给 UI 回调、一次给决策），
所以**模拟器测的从来不是玩家真正看到的那三张牌**。现在 offer 发布在 `G.offer` 上，
无头驱动回答的和玩家看到的是同一组。

### 最后一个：暴怒封了顶，于是僵持成立了

跑完整套边界配置时，`--picks focused` 和手机视口各有一局**跑到 900 秒硬停都没结束**。

v1.0 我加过一条不变量：「任何一局都不许跑到硬停时限——那意味着弧线没有终止」。
这次它抓到了自己该抓的东西。

原因是 `e.rage = Math.min(1, ...)`。封顶之后 Boss 不再变强，
于是一个**打不动它、但也死不掉**的 build 可以无限僵持下去——
这正是暴怒这个机制当初被设计出来要消灭的东西，而我亲手给它加了个上限。

改成不封顶：前 `rageRamp` 秒的曲线一字未动（调好的那场仗不受影响），
超出之后进入 overdrive，速度、接触伤害、弹幕密度继续爬。
`focused` 的通关率从 37.5% 掉到 18.8%——**拖延流被正确地杀死了。**

之后用 11 组配置 384 局做了一次专门针对这条不变量的扫描，全部通过。
